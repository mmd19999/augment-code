{
  "name": "task-manager-backend",
  "version": "1.0.0",
  "description": "Enhanced Task Manager Backend API with improved database management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:debug": "nodemon --inspect server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:health": "node -e \"require('./config/database').healthCheck().then(console.log)\"",
    "db:stats": "node -e \"require('./utils/database').checkCollectionHealth(require('./models/Task')).then(console.log)\"",
    "lint": "echo \"Linting not configured yet\"",
    "format": "echo \"Formatting not configured yet\""
  },
  "keywords": [
    "express",
    "mongodb",
    "mongoose",
    "task-manager",
    "api",
    "nodejs",
    "backend"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^4.18.2",
    "mongoose": "^8.16.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
